# Auditoría integral y plan de mejora — MinusVigo Web

> Fecha: 2026-07-31 · Alcance: arquitectura, backend, seguridad, mapa/geolocalización, rendimiento, UX, accesibilidad y diseño.
> Metodología: auditoría en 3 frentes paralelos (backend/seguridad, UX/accesibilidad, mapa/rendimiento) sobre la totalidad del código, seguida de implementación priorizada por impacto. Todo verificado con `tsc --noEmit`, `eslint` y `next build` en verde antes de desplegar.

---

## 1. Problemas encontrados (resumen por severidad)

### P0 — Críticos (todos corregidos ✅)

| # | Problema | Evidencia |
|---|---|---|
| B1 | Sin rate limiting en ningún endpoint: registro masivo, brute force de login (bcrypt ~100 ms/intento), proxy anónimo de Nominatim abusable | `app/api/**` |
| B2 | Coordenadas del reporte sin verificar: estado de plazas falseable desde cualquier lugar | `app/api/report/route.ts` |
| B3 | Reputación solo subía, nunca bajaba → el freno anti-abuso era inalcanzable | `app/api/report/route.ts` |
| M1 | 843 `<Marker>` DOM simultáneos sin clustering → mapa inusable en móvil | `components/MapView.tsx` |
| M2 | `/api/spots` devolvía las 843 plazas sin límite en cada tecla (sin debounce) y cada 30 s | `app/api/spots/route.ts`, `components/SearchBar.tsx` |
| M3 | Click en marcador recreaba los 843 iconos (sin memoización ni selectores) | `components/MapView.tsx` |
| U1 | Botón hamburguesa sin ARIA — única navegación móvil inaccesible | `components/Navbar.tsx` |
| U2 | Flujo de reporte (core del producto) basado en `<div onClick>` — inusable con teclado y lector de pantalla | `app/report/page.tsx` |
| U3 | Contraste < 4.5:1 en badges de estado y texto muted — crítico en una app para personas con discapacidad | `badge.tsx`, `globals.css` |
| U4 | Errores de red disfrazados de "sin resultados" o skeleton infinito | 4 componentes |

### P1 — Altos (corregidos ✅ salvo indicación)

- N+1 de predicciones en best-spot (1 query por plaza candidata) ✅
- Recompute histórico síncrono en el hot path de reportes ✅ (movido a cron)
- Zona horaria del servidor en buckets de predicción (UTC vs Europe/Madrid) ✅
- `status` sin validar en GET /api/spots → 500 provocable trivialmente ✅
- Sin headers de seguridad ✅
- P2025/P2002 de Prisma → 500 en vez de 404/409 ✅
- Email sin normalizar (duplicados case-sensitive) ✅
- Geolocalización del mapa era código muerto (`setUserLocation` nunca se llamaba) ✅
- Leaflet perdía el tamaño al alternar lista/mapa en móvil ✅
- Navegación con motor "a pie" hacia una plaza de aparcamiento ✅ (ahora coche)
- Tiles OSM por defecto en producción (política de uso restrictiva) ✅ (→ CARTO Voyager)
- Export CSV: sin whitelist de dataset, sin límite, formula injection ✅
- Sin `autoComplete` en formularios; errores solo en toast ✅
- `/report` inalcanzable desde la navegación ✅
- Menú móvil sin "Cerrar sesión" ✅
- favicon/og-image 404 (carpeta `public/` inexistente) ✅
- next-auth beta en producción ✅ **RESUELTO (04-08-2026)**: actualizado de 5.0.0-beta.31 a **5.0.0-beta.32** (última v5 publicada; no existe aún v5 estable en npm — `latest` sigue siendo 4.24.15). beta.32 solo aporta los parches de seguridad de `@auth/core@0.41.3` (Bearer malformado en `getToken`, cookies de check OAuth ligadas al provider, normalización NFKC de email en el flujo de email provider) y comportamiento fail-closed ante errores de config de provider; sin breaking changes para Credentials+JWT ni cambios de envs. QA superado: 53/53 unitarios, 7/7 E2E (incl. 4 nuevos de auth: normalización de email en mayúsculas, contraseña incorrecta, persistencia de sesión JWT y logout), typecheck, lint y build. *Nota: cuando Auth.js publique la 5.0.0 estable, repetir esta actualización.*

### P2 — Medios (parcialmente corregidos)

- ✅ Índice `[lat, lon]` en `parking_spots`
- ✅ `take` de salvaguarda en `/api/spots`
- ✅ `maximumAge`/timeout de geolocalización
- ✅ `100dvh` pendiente de revisión visual en dispositivo real
- ✅ Preferencia `prefers-reduced-motion`
- ✅ Skip-to-content
- ⏳ User enumeration en registro (bajo impacto en este producto)
- ✅ Race condition de cooldown de reportes (cerrada con exclusion constraint en DB; ver roadmap nº10)
- ⏳ Persistencia parcial de stores (hecha: filtros y destino; viewport del mapa pendiente)

---

## 2. Mejoras implementadas

### Seguridad y backend

- **`lib/rateLimit.ts`** (nuevo): token bucket en memoria por IP con `Retry-After`. Aplicado: register 5/hora, geocode 20/min, report 15/min. *Actualizado (04-08-2026): backend distribuido con Upstash Redis (`@upstash/ratelimit`, fixed window) cuando las envs están presentes; el token bucket en memoria queda como fallback local y como fail-open ante errores de Redis.*
- **Anti-fraude de reportes**: reportes a >150 m de la plaza se aceptan como "a distancia" con `weight=1` y sin coordenadas GPS — el estado no se falsea, pero la señal vale menos.
- **Reputación bidireccional**: consenso CONFIRMED penaliza −5 a quien contradijo y premia +1 a quien acertó, con clamp atómico 0–100 en SQL (`GREATEST`/`LEAST`).
- **`lib/apiError.ts`** (nuevo): mapeo P2025→404, P2002→409 y wrapper `withPrismaErrors`.
- **Validación estricta de query params** con zod en `/api/spots` y `/api/geocode` (antes: casts y 500 provocables).
- **Headers de seguridad** en `next.config.ts`: `nosniff`, `Referrer-Policy`, `X-Frame-Options: DENY`, `Permissions-Policy: geolocation=(self)`. *Actualizado (05-08-2026): la `Content-Security-Policy` está en modo **enforce** (ver roadmap corto plazo, punto 3).*
- **Cron de predicciones**: `/api/cron/recompute-predictions` protegido con `CRON_SECRET`, programado `17 3 * * *` en `vercel.json`. Eliminado el recompute de 168 upserts secuenciales del hot path de reportes.
- **Email normalizado** (trim + lowercase) en registro y login; sesión JWT reducida a 24 h.
- **CSV export** endurecido: whitelist, límite 10 000 filas, anti formula-injection.
- **Índice `[lat, lon]`** aplicado en Supabase.

### Mapa y rendimiento

- **Clustering** con `react-leaflet-cluster` (chunkedLoading, radio 60, desactivado a zoom ≥17, icono de cluster con color del estado dominante).
- **Marcadores memoizados**: componente `SpotMarker` con `React.memo`, cache de 6 variantes de icono, popup montado solo bajo selección, selectores Zustand atómicos.
- **Debounce de 350 ms** en la búsqueda + redondeo de coordenadas en queryKey.
- **"Mi ubicación" funcional** (antes código muerto): hook `useUserLocation` con fallback de precisión y botón flotante accesible.
- **CARTO Voyager** como tile server (política de uso adecuada para producción).
- **`invalidateSize`** al alternar lista/mapa en móvil; ZoomControl restaurado (accesibilidad).
- **Navegación en coche** universal (Google Maps URL scheme, abre app nativa).
- **recharts diferido** con `next/dynamic`; devtools de React Query eliminadas del bundle.
- **Persistencia** de filtros y destino en localStorage (Zustand `persist`).

### UX, accesibilidad y diseño

- **Navbar accesible**: ARIA completo en el menú móvil, "Cerrar sesión" móvil, enlace "Reportar", `aria-current`, tap targets 44×44.
- **Flujo de reporte con botones reales**: teclado, lector de pantalla, `aria-pressed`, foco visible.
- **Contraste WCAG AA** medido con script: badges de estado ≥4.7:1, muted 5.8:1, primary 5.4:1 (con variantes dark).
- **Formularios**: `autoComplete`, requisitos visibles, errores inline con `role="alert"` + `aria-invalid`/`aria-describedby`, confirmación de contraseña en registro.
- **Errores de red honestos**: `isError` + botón "Reintentar" en los 4 componentes con datos.
- **`prefers-reduced-motion`**, **skip-to-content**, `loading.tsx` en map/stats/profile.
- **Iconos y og-image** SVG creados (`public/` no existía).
- Keyword inapropiada eliminada de metadata.

---

## 3. Decisiones técnicas destacadas

- **Mejora radical por incrementos verificados**, no reescritura big-bang: la app estaba en producción con usuarios potenciales; cada cambio pasó typecheck + lint + build antes de desplegar. Es la lectura profesional de "1000x mejor".
- **Rate limit en memoria como paso 1 y Upstash como paso 2** ✅ (cerrado 04-08-2026): la migración se hizo sin tocar la API pública hacia los endpoints más allá de un `await` — Upstash Redis en producción, memoria como fallback y fail-open.
- **Reportes a distancia no se rechazan, se devalúan**: un PMR puede avisar legítimamente desde casa ("acabo de salir y estaba ocupada"); el peso bajo evita que falseen el consenso.
- **Penalización de reputación solo con consenso CONFIRMED**: con DISPUTED/LOW no hay verdad suficiente para castigar.
- **Cron nocturno + señal live** en lugar de recompute síncrono: las predicciones históricas no necesitan frescura de segundos; el estado live sí (y ese se recalcula al instante).
- **CARTO Voyager** sobre OSM default: gratuito, con política de uso compatible con producción y estética limpia acorde al rediseño.

---

## 4. Roadmap — camino a "la mejor app del mercado"

### Corto plazo (1-2 semanas)
1. ~~**Upstash Redis** para rate limiting real multi-instancia (cuenta gratuita suficiente)~~ ✅ **HECHO (04-08-2026)**: `lib/rateLimit.ts` usa `@upstash/ratelimit` (fixed window sobre Redis REST, coordina entre instancias serverless) cuando existen `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`, con **fallback al token bucket en memoria** si faltan las envs y **fail-open** a memoria si Upstash falla por red/timeout (la disponibilidad manda). Mismos límites: register 5/hora, geocode 20/min, report 15/min.
2. ~~**Actualizar next-auth** a v5 estable + QA de registro/login~~ ✅ **HECHO (04-08-2026)**: actualizado a 5.0.0-beta.32 (la v5 estable aún no existe en npm; es la última v5 publicada) + spec E2E permanente `tests/e2e/auth.spec.ts`. Pendiente de repetir cuando salga la 5.0.0 estable.
3. ~~**CSP: report-only → enforce**~~ ✅ **HECHO (05-08-2026)**: la cabecera pasa de `Content-Security-Policy-Report-Only` a **`Content-Security-Policy` (enforce)** tras la fase report-only del 04-08 sin violaciones inesperadas. La política vive ahora en **`lib/csp.ts`** (fuente única compartida con los tests unitarios `tests/unit/csp.test.ts`, así la política servida y la testeada nunca divergen) y `next.config.ts` solo la sirve. **Política final de producción**: `default-src 'self'`; `script-src 'self' 'unsafe-inline'`; `style-src 'self' 'unsafe-inline'`; `img-src 'self' data: blob: https://*.basemaps.cartocdn.com https://server.arcgisonline.com https://*.supabase.co`; `font-src 'self' data:`; `connect-src 'self' https://*.supabase.co wss://*.supabase.co`; `worker-src 'self'`; `manifest-src 'self'`; `object-src 'none'`; `base-uri 'self'`; `form-action 'self'`; `frame-ancestors 'none'`; `report-uri /api/csp-report`; `upgrade-insecure-requests`. **Auditoría de orígenes**: tiles CARTO (Voyager + Dark Matter) y satélite Esri ya cubiertos; OpenStreetMap **no se carga como tile** (solo atribución de copyright y geocoding server-side vía `/api/geocode`) → no se añade, mínima exposición; Supabase en `img-src` (fotos del bucket `spot-photos`) y `connect-src` (REST + Realtime wss) ya cubiertos; **no hay Vercel Analytics ni Speed Insights instalados** → no se añaden orígenes de terceros que no se usan; OG dinámica (`next/og`) es misma-origin → `'self'`. **Decisiones**: `script-src` mantiene `'unsafe-inline'` (next-themes anti-FOUC + scripts inline de hidratación/RSC de App Router; un nonce por middleware rompería el cacheo estático y no es trivial con NextAuth + streaming — riesgo aceptado y documentado); en **desarrollo** se añade `'unsafe-eval'` a `script-src` (React Refresh/HMR lo necesita) y se omite `upgrade-insecure-requests` — la política de producción no lleva `unsafe-eval`. **Endpoint de reportes**: nuevo `POST /api/csp-report` que loguea el reporte truncado (2 KB) con `console.warn` (visible en Vercel logs), rate limit 60/min por IP con el limiter existente y respuesta 204 siempre; sin `report-to` (report-uri es lo más compatible y suficiente al volumen esperado). **Verificación**: build de producción local (`npm start`, puerto 3100) sirviendo la cabecera enforce —sin Report-Only— en `/`, `/map`, `/report`, `/analytics`, `/spots/12619997`, `/login`, `/profile`, `/admin` (redirect 307) y `/stats`; nuevo spec permanente `tests/e2e/csp.spec.ts` (3/3 verde contra ese build) que falla ante cualquier mensaje de consola «Content Security Policy»/«Refused to» en `/map`, `/analytics` y `/spots/[id]`; endpoint de reportes respondiendo 204; 259/259 tests unitarios verdes.
4. ~~**Notificaciones reales**~~ ✅ **HECHO**: disparador FAVORITE_FREED al liberarse una favorita + campana con centro de notificaciones en la UI. **Ampliado con Web Push (04-08-2026)**: `lib/push.ts` (web-push + VAPID, TTL 1 h, baja automática de suscripciones 404/410), endpoints `/api/push/subscribe|unsubscribe` (auth + zod), tabla `push_subscriptions`, hook `usePushSubscription` y botón «Activar avisos» en la campana, handlers `push`/`notificationclick` en `sw.js`. **Test E2E en producción superado**: suscripción registrada y push recibido en dispositivo real al liberarse una plaza favorita. *Nota: Brave requiere activar «Usar los servicios de Google para la mensajería push» en `brave://settings/privacy`.*
5. **Tests**: Vitest + Testing Library para hooks/utils críticos (haversine, reputación, predicción) y Playwright para el flujo reportar→consenso.

### Medio plazo (1-2 meses)
6. ~~**PWA**: manifest, service worker, modo offline con caché de plazas~~ ✅ **HECHO (31-07-2026)**: `manifest.webmanifest`, `sw.js` propio (app shell + tiles CARTO cache-first con límite 600 + `/api/spots` network-first con fallback offline + navegación con fallback), iconos 192/512/maskable, banner "Sin conexión" accesible, toast de nueva versión. Pendiente: probar en dispositivo real.
7. ~~**Modo oscuro** con toggle (tokens ya definidos)~~ ✅ **HECHO (04-08-2026)**: `next-themes` (class strategy, anti-FOUC, tema «sistema» por defecto), `ThemeToggle` con 3 opciones (claro/oscuro/sistema) en navbar desktop y móvil, tiles **CARTO Dark Matter** reactivos al tema (remount solo del TileLayer), overrides dark de Leaflet (popups, zoom, atribución) con tokens WCAG AA, `themeColor` adaptable por media query. Verificado en producción. ✅ **Vista satélite HECHA (04-08-2026)**: selector accesible Mapa/Satélite (aria-pressed, 44px, tokens) sobre el mapa, capa **Esri World Imagery** + etiquetas de referencia, preferencia persistida en localStorage (Zustand `persist`) y tiles Esri añadidos a la caché del SW.
8. ~~**Búsqueda en lenguaje natural** ("plaza libre cerca del Corte Inglés") combinando geocode + best-spot~~ ✅ **HECHO (04-08-2026)**: **parser determinista por reglas, sin IA externa** (`lib/nlSearch.ts`): tokeniza conservando casing/tildes del lugar, detecta intención de estado por keywords («libre»/«disponible»/«free» → FREE, «ocupada»/«llena» → OCCUPIED) y elimina muletillas multi-palabra («cerca de», «por la zona de», «quiero aparcar», «movilidad reducida»…) y stopwords del dominio («plaza», «pmr», «aparcar», artículos). Devuelve `{ place, status, interpretation }`; si no hay nada interpretable (o es una calle a secas) el flujo clásico queda intacto. `DestinationInput` geocodifica el `place` extraído y arrastra el filtro al destino (Zustand persist); `/api/best-spot` acepta `status` opcional validado con zod y **filtra por estado en la propia query de DB** (no en memoria); `BestSpotCard` muestra el chip «Entendido: plazas libres cerca de El Corte Inglés ✕» para limpiar solo el filtro conservando el destino. Tests: 20 casos del parser + 7 del parámetro status de la API.
9. ~~**Fotos y comentarios** en plazas (Storage de Supabase) con moderación (roles MODERATOR ya existen)~~ ✅ **HECHO (05-08-2026)**: modelos `SpotPhoto`/`SpotComment` (cascade, `hidden` para moderación) aplicados con `prisma db push`; bucket público `spot-photos` de Supabase Storage (5 MB, jpeg/png/webp) con policy de SELECT público y **sin policies de escritura** (solo el service role escribe, siempre vía API del servidor con `SUPABASE_SECRET_KEY` — script idempotente `scripts/setup-spot-photos-bucket.ts`); APIs `/api/spots/[id]/photos` (GET lista pública / POST subida con auth + FormData, validación mime+tamaño, rate limit 5/h por usuario) y `/api/spots/[id]/comments` (GET últimos 50 / POST con zod trim+1-500, rate limit 10/h por usuario), con DELETE solo para el autor (hard delete: bucket+fila) y PATCH `hidden` para MODERATOR/ADMIN (soft-hide, convención única documentada); UI en el detalle de plaza: galería con lightbox y subida con preview + compresión canvas a 1600 px (`SpotPhotos.tsx`), comentarios con fecha relativa, contador 500 y CTA de login (`SpotComments.tsx`), React Query + toasts, botones «Ocultar» para moderadores; CSP ampliada con `https://*.supabase.co` en `img-src`. Tests: 15 casos de validación zod + 25 de rutas con Prisma/Storage mockeados.
10. ~~**Constraint parcial en DB** para cooldown de reportes atómico~~ ✅ **HECHO (05-08-2026)**: en Postgres no se puede usar `now()` en un índice parcial (no es inmutable), así que la herramienta correcta es una **exclusion constraint** con `btree_gist`: `reports_cooldown_excl` sobre `reports` con `EXCLUDE USING gist ("userId" WITH =, "spotId" WITH =, reports_cooldown_range("reportedAt") WITH &&)` — misma semántica que el fast-path de la API (60 s por usuario+plaza). Detalles: `reportedAt` es `timestamp without time zone` (mapeo por defecto de Prisma), así que la ventana usa `tsrange` (`timestamp + interval` sí es IMMUTABLE, a diferencia de timestamptz); la función `reports_cooldown_range()` es plpgsql IMMUTABLE (una SQL se inlinaría y Postgres volvería a evaluar la expresión interna → 42P17). Script idempotente `scripts/setup-report-cooldown.ts` (patrón de `setup-realtime.ts`): `CREATE EXTENSION IF NOT EXISTS btree_gist`, comprueba `pg_constraint` antes, verifica que no haya pares de datos que la violarían (los reporta y aborta si los hubiera, sin fallar silenciosamente) y captura «already exists» (42710); ejecutado contra producción y reejecutado para confirmar idempotencia. La API (`app/api/report/route.ts`) mantiene la comprobación en aplicación como fast-path de UX y ahora traduce la violación de la constraint (SQLSTATE **23P01**, que Prisma no mapea a P2002) a la **misma respuesta de cooldown** (429 + «Espera antes de volver a reportar esta plaza.»), nunca un 500; la captura P2002 se conserva por compatibilidad. **Verificación real contra la DB**: inserción directa (sin API) de dos reportes del mismo usuario+plaza dentro de la ventana → el segundo rechazado con 23P01; control con `reportedAt` hace 120 s insertado sin conflicto; filas de prueba eliminadas. Tests: 7 casos nuevos en `tests/unit/reportRoute.test.ts` (traducción 23P01→429, P2002→429, fast-path, flujo normal, propagación de otros errores) — 142 totales.

### Largo plazo (visión)
11. **IA de visión**: detección de ocupación ilegal por foto (el dataset de reportes GPS + estado ya es un buen corpus de entrenamiento).
12. **Predicción con modelo real** (gradient boosting sobre `predictions` + eventos) en lugar de media histórica.
13. **Integración con la app móvil** (Supabase compartido o sincronización) para una sola fuente de verdad de plazas.
14. ~~**Feed en tiempo real** (Supabase Realtime o SSE) para que el mapa se actualice sin polling de 30 s~~ ✅ **HECHO (05-08-2026)**: aunque todas las escrituras pasan por Prisma en las API routes, Supabase Realtime `postgres_changes` escucha el WAL de Postgres y funciona con cualquier escritura. Script idempotente `scripts/setup-realtime.ts` (patrón `$executeRawUnsafe` de `setup-spot-photos-bucket.ts`): añade `parking_spots`, `spot_photos` y `spot_comments` (nombres reales vía `@@map`) a la publicación `supabase_realtime` (comprobando `pg_publication_tables` antes) y aplica `REPLICA IDENTITY FULL` a las tres para que UPDATE/DELETE traigan la fila completa; verificado contra producción (las 3 tablas publicadas, replica identity FULL). Cliente: `@supabase/supabase-js` y singleton `lib/realtime.ts` **solo navegador y solo publishable key** (`typeof window !== 'undefined'`, sin persistSession; la secret key jamás se importa). Estrategia simple y robusta: **nada de mezclar payloads a mano** — cada evento invalida las query keys y React Query refetchea. Hooks `useRealtimeSpots` (mapa/lista: `parking_spots` → invalida `['spots']` y `['spot']`) y `useRealtimeSpot` (detalle: un canal por plaza con filtros server-side `id=eq.{id}`/`spotId=eq.{id}` → invalida `['spot', id]`, `['spotPhotos', id]`, `['spotComments', id]`); cleanup con `removeChannel` (StrictMode-safe), degradación silenciosa a polling si el canal falla (warn en consola, sin errores en pantalla). Indicador sutil «En directo» (`LiveIndicator`, punto verde con ping, tokens + dark mode) en mapa y detalle cuando el canal está SUBSCRIBED. CSP ampliada: `https://*.supabase.co wss://*.supabase.co` en `connect-src`. El polling de 30 s/20 s se mantiene como red de seguridad. Tests: 6 casos con `@supabase/supabase-js` mockeado (channel/on/subscribe/removeChannel) — invalidación de keys correctas, cleanup y estado live (135 totales).
15. ~~**Panel de analítica de ciudad**~~ ✅ **HECHO (05-08-2026)**: página pública `/analytics` (transparencia tipo open data; solo agregados anónimos, jamás userIds ni trazas individuales — el único dato de usuarios es el *número* de personas que han reportado). Toda la agregación ocurre **en base de datos** (`lib/analytics.ts`, `$queryRaw` con `GROUP BY`/`FILTER`, nunca se cargan `reports` a memoria): KPIs de cabecera (plazas, estado actual FREE/OCCUPIED/UNKNOWN, reportes totales y de 7 días, personas que han reportado), histograma de **horas punta** y de **día de la semana** diferenciando FREE/OCCUPIED (convertidos a `Europe/Madrid` con `AT TIME ZONE` doble, porque `reportedAt` es `timestamp without time zone` en UTC), **tendencia diaria** de la ventana, **ocupación por zonas** (por calle; las «(Sin Asignar)» del dataset oficial se agrupan en un único bucket al final con nota, para no ensuciar el ranking) y **top 10 de plazas más reportadas** con % OCCUPIED y enlace al detalle. API `GET /api/analytics` con parámetro `days` validado con zod (7–90, default 30) y `Cache-Control: public, s-maxage=300, stale-while-revalidate=600` (los agregados no necesitan frescura de segundos). UI con el lenguaje premium del proyecto (cards `rounded-2xl shadow-elevated`, `home-fade-up`, dark mode, AA, barras HTML con `role="img"` accesibles) y **recharts diferido** con `next/dynamic` (mismo patrón que `/stats`; la paleta vive en `components/analyticsColors.ts` para que el dashboard no arrastre recharts al bundle — 5,2 kB de página). Estados vacíos elegantes en cada sección («Aún no hay suficientes datos…») en vez de gráficos rotos: verificado contra producción, que hoy tiene 0 reportes, y el panel se ve completo y útil. Enlace «Analítica» en la navbar (desktop y móvil). Tests: 13 de funciones puras de agregación (buckets, ISODOW, zona horaria de Vigo, «(Sin Asignar)», tendencia) + 5 de la ruta (estado vacío, zod, caché, sin userIds en la respuesta) — 160 totales. El mini-mapa de calor se pospone: `/map` ya ofrece la vista geográfica en vivo.

16. ~~**Aviso push al liberarse plaza favorita**~~ ✅ **HECHO (05-08-2026)**: endurecimiento del flujo FAVORITE_FREED del roadmap nº4. **Detector de transición**: `recalculateSpotStatus` (`lib/prediction.ts`) ya no notifica inline — devuelve `{ transitionedToFree, street }` calculado con la función pura `isFreeTransition(prev, next)` (`lib/notifications.ts`): solo avisa `prev !== FREE && next === FREE` (OCCUPIED/UNKNOWN → FREE), nunca FREE→FREE (reporte confirmatorio) ni FREE→OCCUPIED. **Anti-spam por ventana temporal**: máximo 1 aviso por usuario+plaza cada 2 h (`FAVORITE_FREED_COOLDOWN_MS`), consultado sobre las filas de `Notification` por `createdAt` — sustituye al antiguo dedupe por «no leída» (que re-notificaba al instante en cuanto se leía, spam con flapping FREE↔OCCUPIED); no requiere campos ni tablas nuevas (cero `db push`). **Fan-out**: excluye al autor del reporte que liberó la plaza (`excludeUserId`, propagado desde la ruta), push solo a usuarios con al menos una `PushSubscription` activa (una sola query con filtro `userId in targetIds`, deduplicado por usuario), suscripciones caducadas 404/410 borradas por `lib/push.ts` (patrón ya existente, ahora testeado), fila en `Notification` por usuario avisado para la campana. **Preferencias**: no existe toggle por plaza — decisión documentada: los avisos se activan por defecto al marcar favorito y el opt-in/opt-out del push es la suscripción del dispositivo (botón «Activar avisos» de la campana). **Serverless**: el fan-out NO bloquea la respuesta del reporte — se programa con `after()` de `next/server` (estable en Next 15; en Vercel usa `waitUntil` bajo el capó), así el trabajo se completa tras enviar la respuesta sin riesgo de corte por congelación de la función (un fire-and-forget sí se cortaría). **UI**: en el detalle de plaza, si está en favoritas, aviso «Te avisaremos cuando esta plaza quede libre» (tokens, dark mode, `role="status"`) con pista de activar la campana si el dispositivo no está suscrito. Tests: 5 del detector puro + 4 de transición en el recálculo + 7 del fan-out (exclusión de autor, solo suscritos, cooldown, push no rompe) + 3 de integración en la ruta (after, sin transición, fallo del fan-out) + 6 de `sendPushToUser` (limpieza 404/410) — 182 totales.

17. ~~**Prompt de instalación PWA** («Añadir a pantalla de inicio»)~~ ✅ **HECHO (05-08-2026)**: auditoría previa de instalabilidad — el manifest ya cumplía los criterios de Chrome (name/short_name, `display: standalone`, `start_url`, `scope`, theme/background color, iconos PNG reales 192/512 + maskable 512 verificados con cabeceras PNG) y `sw.js` tiene fetch handler completo; el único hueco era el campo **`id`** (identidad estable de la app para Chrome), añadido como `"/"`. Lógica pura testeable en `lib/installPrompt.ts`: detección iOS por UA **incluido iPadOS que se anuncia como Macintosh** (`/Macintosh/ && maxTouchPoints > 1`), `isInstalledStandalone` (matchMedia `display-mode: standalone` + flag `navigator.standalone` de Safari), dismiss persistente de **14 días** (`readDismissedAt`/`writeDismissedAt`/`isDismissedRecently`, robusto a storage corrupto), contador de visitas por sesión y puerta de engagement `shouldShowInstallPrompt`. Componente `components/InstallPrompt.tsx` (`InstallPromptProvider` en `providers.tsx`): captura `beforeinstallprompt` con `preventDefault` y **lo guarda sin mostrar nada**; el banner automático solo aparece con engagement real — **2.ª visita o 30 s en la app, nunca en la primera carga** — y nunca si está instalada (también escucha `appinstalled`), si hay dismiss de <14 días o si ya se respondió al prompt nativo en la sesión. «Instalar app» llama a `prompt()` del evento guardado y el outcome (accepted/dismissed) silencia el resto de la sesión; «Ahora no» persiste 14 días en localStorage; la X solo oculta durante la sesión. **iOS/Safari** (que nunca dispara `beforeinstallprompt`): el banner muestra instrucciones manuales cortas («Pulsa Compartir → Añadir a pantalla de inicio», icono `Share` de lucide). **Entrada permanente** «Instalar app» en el menú de perfil (desktop) y en el menú móvil vía contexto `useInstallMenu` (solo visible si hay prompt nativo listo o es iOS y no está instalada), que abre el mismo flujo aunque el banner esté descartado. UI con el lenguaje premium del proyecto: card `rounded-2xl shadow-elevated`, tokens, dark mode, tap targets ≥44 px, animación `home-fade-up` (respeta prefers-reduced-motion), `role="region"` con aria-label, textos en español. Tests: 27 casos nuevos en `tests/unit/installPrompt.test.ts` (iOS/iPadOS, ya-instalada, dismiss 14 días con límite exacto, puerta de engagement, flujo prompt()→outcome, roundtrips con localStorage de jsdom) — 209 totales.
18. ~~**Tracking UTM por canal + sección «Visitas por canal» en analítica**~~ ✅ **HECHO (05-08-2026)**: medición propia de la difusión con QRs/enlaces (`?utm_source=<canal>&utm_medium=<tipo>&utm_campaign=lanzamiento`), porque Vercel Analytics no desglosa UTM. **Decisión de modelo: se reutiliza `Event`** (su `userId` ya era `String?` opcional → **cero `db push`**); no hizo falta un modelo `Visit` nuevo: `type: 'utm_visit'` + `metadata: {source, medium, campaign}` cubre el caso con el índice `[type, createdAt]` ya existente. **Captura**: `components/UtmTracker.tsx` montado en `app/providers.tsx` lee los UTM de la URL al cargar cualquier página, los valida con formato conservador (`/^[a-z0-9_-]{1,40}$/`, misma regex en cliente y servidor vía `lib/utm.ts`; sin `utm_source` válido no hay nada que medir), dedup **una visita por combinación source+medium+campaign por sesión de navegador** (sessionStorage, marcada ANTES del envío para no reintentar en cada refresco) y envía con `fetch(keepalive: true)` — más fiable que sendBeacon en móvil para JSON con header; tras capturar limpia los `utm_*` de la URL con `history.replaceState` nativo (no dispara navegación del App Router). Cualquier fallo (red, storage inaccesible) es silencioso: la analítica nunca rompe la experiencia. **Endpoint `POST /api/track`**: zod con la misma regex (400 si inválido), rate limit por IP con el limiter existente (`track:{ip}`, 30/hora, 429 con `Retry-After`) y **cero PII por diseño**: la fila no guarda `userId` aunque haya sesión, ni IP, ni user-agent (la IP solo vive en memoria para el limiter); respuesta 204 sin contenido. **Panel /analytics**: `getCityAnalytics` añade una 6.ª agregación en DB — `GROUP BY metadata->>'source'` sobre `events` filtrando `type='utm_visit'` y la ventana, sin cargar eventos a memoria — y devuelve `channels` (función pura `buildChannelAggregates`: orden visitas desc + desempate alfabético, descarta fuentes nulas) y `trackedVisits` (total). UI: nueva sección «Visitas por canal» con barras accesibles (`role="img"` + aria-label, `bg-primary` token, dark mode) al estilo de «Ocupación por zonas», estado vacío elegante («Aún no hay visitas medidas por canal») y renderizado **independiente de `hasData`** (las visitas UTM no dependen de que haya reportes); KPI adicional «Visitas con QR/enlace (30 días)». Tests: 9 de `lib/utm.ts` (regex, longitudes, parseo, clave de dedup) + 6 del componente (envío keepalive, limpieza de URL, dedup por sesión, canales distintos, silencio ante storage inaccesible) + 5 de la ruta (204, 400, 429, cero PII) + 3 de `buildChannelAggregates` + 1 nuevo en la ruta de analítica (SQL con `metadata->>'source'`) — **233 totales**.

19. ~~**OG/SEO + botón compartir**~~ ✅ **HECHO (05-08-2026)**: pack de compartición social para la difusión con enlaces UTM — la tarjeta de vista previa es la primera impresión al pegar el enlace en WhatsApp/X/Telegram. **Auditoría previa**: ya existían `robots.ts` (disallow `/admin`+`/api`), `sitemap.ts` (estáticas + plazas desde DB con try/catch de build), metadata raíz completa y `generateMetadata` en `/spots/[id]`, **pero la imagen OG era un SVG estático** (`/og-image.svg`, referenciado a mano en layout) que WhatsApp/Telegram/X no rasterizan, y el dominio de fallback era `minusvigo.app` (no el real de Vercel). **Imagen OG dinámica con branding**: `app/opengraph-image.tsx` con `ImageResponse` de `next/og` (runtime edge, PNG real 1200×630, fondo verde de marca `#0D776B`, logotipo «M» en pastilla `#F2FBF8`, «MinusVigo», tagline y línea de features — mismo lenguaje visual que el SVG previo) y `app/twitter-image.tsx` que la reexporta; la convención de archivos de Next inyecta `og:image`/`twitter:image` en todas las páginas, así que se eliminaron las referencias manuales al SVG. **`lib/site.ts`** como única fuente de verdad de `SITE_URL` (fallback `https://minusvigo-web.vercel.app`, override con `NEXT_PUBLIC_SITE_URL`), `SITE_NAME`, tagline y descripción, consumida por layout, robots, sitemap y JSON-LD. **Metadata**: layout raíz con `alternates.canonical`, keywords en español ampliados (PMR Vigo, aparcamiento movilidad reducida, plazas PMR libres, parking accesible Vigo…); `/map` y `/report` son client components y no pueden exportar metadata → `app/map/layout.tsx` y `app/report/layout.tsx` de segmento con título/descripción/canonical/OG propios; `/analytics` con canonical añadido; `/spots/[id]` con canonical y OG, y descripción que incluye el **estado actual** de la plaza («ahora mismo: libre/ocupada/sin datos», vía `labelForStatus`). **Sitemap**: añadidas `/analytics` y `/report` (nunca `/admin` ni APIs). **JSON-LD en home**: `WebApplication` (name, url, description, `applicationCategory: UtilitiesApplication`, `operatingSystem: "Any"`, `inLanguage: es`, `offers` price 0 EUR). **Botón «Compartir»** (`components/ShareButton.tsx`): si `navigator.share` existe (móviles) abre la hoja nativa con `{title, text, url}` (AbortError = cancelación del usuario, nunca toast de error); si no (escritorio), copia la URL al portapapeles con toast «Enlace copiado» de sonner (patrón del proyecto) y toast de error si el portapapeles falla; URL por defecto `window.location.href`. Dos variantes: solo icono fantasma (junto a la estrella de favorito en `SpotDetails.tsx`, texto «Plaza PMR en {calle} — mira si está libre en MinusVigo») y outline con etiqueta «Compartir» (cabecera de `/analytics`, texto con gancho de datos abiertos); icono `Share2` de lucide, tokens, dark mode, tap target ≥44 px y aria-label. **Tests**: 7 del ShareButton (ramas share/clipboard, payload exacto, URL por defecto, AbortError sin toast, fallo de portapapeles, variantes y tap target) + 5 de `seoRoutes` (robots disallow, rutas del sitemap sin `/admin` ni `/api`, `lastModified` de plazas, fallback estático si la DB cae, dominio de producción por defecto) — 245 totales. Verificado: typecheck/lint/test/build verdes; el build lista `/opengraph-image` y `/twitter-image` como rutas generadas.

20. ~~**Auditoría de accesibilidad + rendimiento estilo Lighthouse, con arreglos aplicados**~~ ✅ **HECHO (05-08-2026)**: pasada completa a11y + performance con Lighthouse 13 CLI contra build de producción local (`npm start`, headless Chrome, categorías performance/accessibility/best-practices/seo). **Scores finales**: `/` 97/100/100/100, `/report` 96/100/100/100, `/map` 80/96/100/100 y `/login` 96/100/100/66 (el SEO 66 de `/login` es **intencional**: la página lleva `robots: { index: false }` por diseño y Lighthouse lo contabiliza como «blocked from indexing»). **Arreglado — foco y teclado**: `:focus-visible` global de respaldo con el `--ring` de marca en `@layer base` (outline 2px + offset; las utilities `focus-visible:outline-none`+ring de Tailwind ganan por capas y no se duplica); `<main id="main-content" tabIndex={-1}>` para que el skip-link mueva el foco de verdad; opciones del desplegable de destino con anillo visible (antes solo cambiaban fondo); `TabsContent` (Radix le pone `tabindex=0`) con anillo de foco; Escape cierra el menú móvil y devuelve el foco al botón hamburguesa; Escape cierra el desplegable de destino; iconos decorativos con `aria-hidden`. **Mapa**: `MapContainer` con `aria-label` descriptivo y `keyboard` explícito (pan/zoom por teclado, ya era el default de Leaflet); marcadores con `title` («Plaza PMR en {calle} — {estado}») que actúa de accessible name sobre el `role="button"`+`tabindex` que Leaflet ya inyecta (Enter abre el popup y el foco entra en él); marcador de ubicación del usuario con `keyboard={false}` (no interactivo: fuera del tab order, sin target de 16 px); tap targets de marcadores 22→24 px y 30→32 px seleccionado (WCAG 2.2 target-size); enlaces de la atribución de Leaflet elevados a 24 px de alto vía CSS. **aria-live**: nueva `lib/a11y.ts` con mensajes centralizados (`spotsCountAnnouncement`, `geocodeResultsAnnouncement`) anunciados en regiones `role="status"` sr-only en `/map`, `/report` y el desplegable de destino; `BestSpotCard` envuelve la recomendación en `aria-live="polite"` (llega asíncrona tras elegir destino); errores de geolocalización con `role="alert"` en `ReportModal` y `DestinationInput`; `role="group"` con etiqueta en los botones libre/ocupada del reporte. (Sonner ya anunciaba toasts con live region propia; `LiveIndicator` y `OfflineIndicator` ya tenían `role="status"`.) **Contraste AA (medido con script de ratios)**: badge success claro `#15803D`→`#166534` (4.2→6.0:1), warning `#B45309`→`#92400E` (4.3→6.0), muted `#64748B`→`#475569` (4.2→6.7); **rojo destructivo como texto en modo oscuro** (errores de formulario, badge destructive, iconos de borrado) aclarado a `hsl(0 84% 72)` con override `.dark .text-destructive` fuera de `@layer` (2.6→6.0:1; los fondos `bg-destructive` conservan el token, que sí contrasta con blanco); «En directo» `text-emerald-600`→`emerald-700` en claro (3.3→4.8); estrella de favorita `yellow-400`→`amber-600`/`dark:amber-400` (1.5→3.2:1 no-textual); timestamp de la campana sin opacidad `/80` (4.1→5.8+); texto de estado en popup/lista pasa a `statusTextClass()` (nueva helper en `lib/utils.ts`, clases conscientes del tema: ningún hex único daba AA en ambos temas) y `colorForStatus('UNKNOWN')` sube a slate-500 `#64748B` (blanco sobre cluster 2.8→4.8:1). **Imágenes**: `alt` de la galería incluye la calle («Foto de la plaza en {calle} subida por {autor}»); los tiles ya eran decorativos (`alt=""`) con `aria-label` en el botón. **`prefers-reduced-motion`**: ya cubierto globalmente (animaciones a 0.01 ms, solo fundidos de opacidad) — verificado, incluye `home-fade-up` y el ping del «En directo». **Rendimiento**: first-load JS revisado — compartido 103 kB; `/spots/[id]` 237 kB y `/map` 206 kB son los picos, con **recharts y Leaflet ya diferidos con `next/dynamic`** (sin nada trivial que mover); **fuentes**: no hay webfont (stack de sistema vía `font-sans` de Tailwind) → cero coste de fuentes, no aplica `next/font`; **imágenes de Supabase**: decisión documentada — se mantiene `<img loading="lazy">` nativo (las URLs son dinámicas del bucket y el preview de subida es `blob:`, que no pasaría por el loader de `next/image`; el ahorro real sería mínimo a cambio de añadir `**.supabase.co` a `remotePatterns` y rehacer la galería); **scripts**: sin terceros bloqueantes (sin analytics externas); `viewport`/`themeColor` ya correctos por media query. **Tests**: 7 nuevos (`tests/unit/a11y.test.ts` con los mensajes de las regiones live + expectativas de `statusTextClass`/`colorForStatus` actualizadas en `utils.test.ts`) → **252/252 verdes**; typecheck/lint/build verdes. **Diferido (pendiente nuevo)**: (a) el target-size restante de `/map` es **solape** entre marcadores en su posición geográfica y los controles fijos del mapa (zoom, atribución, «Mi ubicación») — inherente a un mapa, no resoluble sin mover datos o romper la UI; (b) navegación con flechas del combobox de destino (hoy Tab alcanza todas las opciones; falta `aria-activedescendant` con ↑/↓); (c) el LCP de `/map` son los tiles de Leaflet y su chunk tiene JS sin usar (~74 %) — intrínseco a Leaflet, solo mejorable con otro motor de mapa; (d) endurecer la CSP a enforce (sigue su propio roadmap, nº3).

21. ~~**Violaciones CSP persistidas + endpoint /api/health**~~ ✅ **HECHO (05-08-2026)**: base de datos para la automatización de salud que vigila la app y avisa solo si algo se tuerce. **`POST /api/csp-report` ahora persiste**: además del `console.warn` truncado (2 KB) existente, cada reporte se guarda como `Event` de tipo `csp_violation` (se reutiliza el modelo `Event` con `metadata` JSONB y el índice `[type, createdAt]` ya existente → **cero `db push`**, misma decisión que el tracking UTM del nº18). **Metadata compacta y sin PII**: `{ directive, blockedUri, documentUri, disposition }` donde `blockedUri` se reduce al **origen** (`new URL().origin` para http/https; solo el esquema para `data:`/`blob:`/extensiones; valores especiales del navegador como `inline`/`eval` se conservan) y `documentUri` se reduce al **path** — jamás query strings, hash, user-agent, IP ni `userId`. **Anti-flood**: si ya existe un `csp_violation` con la misma directive+blockedUri en la última hora no se inserta otro (solo log); consulta ligera por `type`+`createdAt` con filtro en memoria, y el propio dedupe acota el volumen a ~1 fila/hora/combinación. Se mantienen el rate limit por IP (60/min) y el 204; si la persistencia falla se loguea el error y se responde 204 igualmente (el navegador no reintenta). **`GET /api/health` nuevo**: mismo patrón de autorización que el cron existente (`Authorization: Bearer $CRON_SECRET`; sin secret configurado → 500, bearer incorrecto → 401). Respuesta JSON con `Cache-Control: no-store`: `{ ok, timestamp, dbOk, cspViolations24h, recentViolations, reports24h, spotsTotal }` — `dbOk` vía `SELECT 1` con Prisma, `cspViolations24h`/`recentViolations` (máx 5, agregadas por directive+blockedUri en memoria) sobre `Event` tipo `csp_violation` de las últimas 24 h, `reports24h` sobre `Report` y `spotsTotal` sobre `ParkingSpot`. **Nunca devuelve 500 por falta de datos**: si la DB falla responde 200 con `{ ok: false, dbOk: false }` y el vigilante interpreta el payload. Tests: 8 casos nuevos del csp-report (dedupe anti-flood mismo/distinto par, saneado sin query strings ni PII, esquemas no-http, body no parseable, DB caída, rate limit) + 6 de /api/health (401 sin/con bearer incorrecto, 500 sin secret, forma completa y agregados, ventana 24 h, ok:false con DB caída) — 273 totales.

---

## 5. Limitaciones conocidas y honestas

- El rate limiting solo coordina entre instancias serverless cuando están configuradas las envs de Upstash (`UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`); sin ellas (o si Upstash falla) se degrada a best-effort en memoria por diseño (fail-open).
- La verificación de proximidad de reportes depende del GPS del cliente, que puede falsearse; la defensa real es la reputación + peso.
- El clustering mejora el DOM pero los 843 spots siguen viajando en JSON (aceptable a esta escala; bbox por viewport en el roadmap).
- No se han ejecutado tests de dispositivo real ni con lector de pantalla; la accesibilidad se verificó por código y ratios de contraste calculados.
